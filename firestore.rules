rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Top-level rules: Only authenticated users can read/write data by default.
    // In production, we can restrict this further based on user roles and document IDs.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
